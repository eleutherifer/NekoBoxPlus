diff --git a/conev.c b/conev.c
index f9218cb..e9d40c1 100644
--- a/conev.c
+++ b/conev.c
@@ -58,6 +58,9 @@ struct eval *add_event(struct poolhd *pool, evcb_t cb,
     val->fd = fd;
     val->index = pool->count;
     val->cb = cb;
+#ifdef ANDROID_APP
+    val->injected_udp_fd = -1;
+#endif
 
     #ifndef NOEPOLL
     struct epoll_event ev = { .events = _POLLDEF | e, .data = {val} };
@@ -110,6 +113,12 @@ void del_event(struct poolhd *pool, struct eval *val)
         free(val->host);
         val->host = 0;
     }
+#ifdef ANDROID_APP
+    if (val->injected_udp_fd >= 0) {
+        close(val->injected_udp_fd);
+        val->injected_udp_fd = -1;
+    }
+#endif
     close(val->fd);
     val->fd = -1;
     val->mod_iter = pool->iters;
@@ -389,4 +398,3 @@ void buff_destroy(struct buffer *root)
     }
     LOG(LOG_S, "buffers count: %d\n", i);
 }
-
diff --git a/conev.h b/conev.h
index 02f2597..59b8fdc 100644
--- a/conev.h
+++ b/conev.h
@@ -67,6 +67,9 @@ struct eval {
     int index;
     unsigned long long mod_iter;
     evcb_t cb;
+#ifdef ANDROID_APP
+    int injected_udp_fd;
+#endif
     
     long tv_ms;
     struct eval *tv_next, *tv_prev;
diff --git a/desync.h b/desync.h
index 52728d7..9841de4 100644
--- a/desync.h
+++ b/desync.h
@@ -1,7 +1,9 @@
 #ifndef DESYNC_H
 #define DESYNC_H
 
+#ifndef STR_MODE
 #define STR_MODE
+#endif
 
 #include <stdint.h>
 #include <stddef.h>
diff --git a/error.h b/error.h
index 6ddb75f..07d9996 100644
--- a/error.h
+++ b/error.h
@@ -61,9 +61,15 @@ static int unie(int e)
     #define LOG_E ANDROID_LOG_ERROR
     #define LOG_S ANDROID_LOG_DEBUG
     #define LOG_L ANDROID_LOG_VERBOSE
-    #define LOG(s, str, ...) \
-        __android_log_print(s, "proxy", str, ##__VA_ARGS__)
-    #define LOG_ENABLED 1
+    static void LOG(int s, const char *str, ...) {
+        if (s == LOG_E || params.debug >= s) {
+            va_list args;
+            va_start(args, str);
+            __android_log_vprint(s, "proxy", str, args);
+            va_end(args);
+        }
+    }
+    #define LOG_ENABLED (params.debug >= LOG_S)
 #else
     #define LOG_E -1
     #define LOG_S 1
diff --git a/extend.c b/extend.c
index 2eea4ea..194d469 100644
--- a/extend.c
+++ b/extend.c
@@ -14,6 +14,7 @@
 #endif
 
 #include <string.h>
+#include <stddef.h>
 #include <assert.h>
 
 #include "proxy.h"
@@ -23,6 +24,9 @@
 #include "desync.h"
 #include "packets.h"
 
+#ifdef ANDROID_APP
+extern int byedpi_android_protect_fd(int conn_fd);
+#endif
 
 static int set_timeout(int fd, unsigned int s)
 {
@@ -821,7 +825,11 @@ ssize_t udp_hook(struct eval *val,
 #ifdef __linux__
 static int protect(int conn_fd, const char *path)
 {
-    struct sockaddr_un sa;
+#ifdef ANDROID_APP
+    (void)path;
+    return byedpi_android_protect_fd(conn_fd);
+#else
+    struct sockaddr_un sa = {0};
     sa.sun_family = AF_UNIX;
     strcpy(sa.sun_path, path);
     
@@ -834,7 +842,8 @@ static int protect(int conn_fd, const char *path)
     setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, &tv, sizeof(tv));
     setsockopt(fd, SOL_SOCKET, SO_SNDTIMEO, &tv, sizeof(tv));
     
-    int err = connect(fd, (struct sockaddr *)&sa, sizeof(sa));
+    socklen_t sa_len = (socklen_t)(offsetof(struct sockaddr_un, sun_path) + strlen(sa.sun_path) + 1);
+    int err = connect(fd, (struct sockaddr *)&sa, sa_len);
     if (err) {
         uniperror("connect");
         close(fd);
@@ -868,5 +877,6 @@ static int protect(int conn_fd, const char *path)
     }
     close(fd);
     return 0;
+#endif
 }
 #endif
diff --git a/main.c b/main.c
index 8e4860b..859afa8 100644
--- a/main.c
+++ b/main.c
@@ -44,7 +44,11 @@ fake_udp = {
 };
 
 
+#ifdef ANDROID_APP
+__thread struct params params = {
+#else
 struct params params = {
+#endif
     .await_int = 10,
     
     .ipv6 = 1,
@@ -775,7 +779,7 @@ int parse_args(int argc, char **argv)
             
         case 'p':
             val = strtol(optarg, &end, 0);
-            if (val <= 0 || val > 0xffff || *end)
+            if (val < 0 || val > 0xffff || *end)
                 invalid = 1;
             else
                 params.laddr.in.sin_port = htons(val);
diff --git a/params.h b/params.h
index 67b5041..b99f72a 100644
--- a/params.h
+++ b/params.h
@@ -173,7 +173,11 @@ struct params {
     int pid_fd;
 };
 
+#ifdef ANDROID_APP
+extern __thread struct params params;
+#else
 extern struct params params;
+#endif
 
 extern struct packet fake_tls;
 extern struct packet fake_http;
diff --git a/proxy.c b/proxy.c
index 5b96d3c..21b6999 100644
--- a/proxy.c
+++ b/proxy.c
@@ -42,10 +42,36 @@
 #endif
 
 
+#ifdef ANDROID_APP
+__thread int server_fd;
+#else
 int server_fd;
+#endif
+
+int byedpi_shutdown_server(void) {
+    if (server_fd <= 0) {
+        return -1;
+    }
+    return shutdown(server_fd, SHUT_RDWR);
+}
+
+int byedpi_server_port(void) {
+    if (server_fd <= 0) {
+        return 0;
+    }
+    union sockaddr_u addr;
+    socklen_t addr_len = sizeof(addr);
+    if (getsockname(server_fd, &addr.sa, &addr_len) < 0) {
+        return 0;
+    }
+    if (addr.sa.sa_family == AF_INET6) {
+        return ntohs(addr.in6.sin6_port);
+    }
+    return ntohs(addr.in.sin_port);
+}
 
 static void on_cancel(int sig) {
-    shutdown(server_fd, SHUT_RDWR);
+    byedpi_shutdown_server();
 }
 
 void dump_all_cache(void);
@@ -507,20 +533,31 @@ static int udp_associate(struct poolhd *pool,
     }
     //
     socklen_t sz = sizeof(addr);
-    
-    if (getsockname(val->fd, &addr.sa, &sz)) {
-        uniperror("getsockname");
-        return -1;
+    bool injected_client = false;
+    int cfd = -1;
+#ifdef ANDROID_APP
+    if (val->injected_udp_fd >= 0) {
+        cfd = val->injected_udp_fd;
+        val->injected_udp_fd = -1;
+        injected_client = true;
+        memset(&addr, 0, sizeof(addr));
+        addr.in.sin_family = AF_INET;
+    }
+#endif
+    if (!injected_client) {
+        if (getsockname(val->fd, &addr.sa, &sz)) {
+            uniperror("getsockname");
+            return -1;
+        }
+        addr.in.sin_port = 0;
+        cfd = nb_socket(addr.sa.sa_family, SOCK_DGRAM);
     }
-    addr.in.sin_port = 0;
-    
-    int cfd = nb_socket(addr.sa.sa_family, SOCK_DGRAM);
     if (cfd < 0) {
         uniperror("socket");
         del_event(pool, pair);
         return -1;
     }
-    if (bind(cfd, &addr.sa, SA_SIZE(&addr)) < 0) {
+    if (!injected_client && bind(cfd, &addr.sa, SA_SIZE(&addr)) < 0) {
         uniperror("bind");
         del_event(pool, pair);
         close(cfd);
@@ -544,12 +581,17 @@ static int udp_associate(struct poolhd *pool,
     
     client->flag = FLAG_CONN;
     client->addr = val->addr;
-    client->addr.in.sin_port = 0;
+    client->addr.in.sin_port = injected_client ? htons(1) : 0;
     
-    sz = sizeof(addr);
-    if (getsockname(cfd, &addr.sa, &sz)) {
-        uniperror("getsockname");
-        return -1;
+    if (injected_client) {
+        memset(&addr, 0, sizeof(addr));
+        addr.in.sin_family = AF_INET;
+    } else {
+        sz = sizeof(addr);
+        if (getsockname(cfd, &addr.sa, &sz)) {
+            uniperror("getsockname");
+            return -1;
+        }
     }
     struct s5_req s5r = { 
         .ver = 0x05 
@@ -659,6 +701,37 @@ static int on_accept(struct poolhd *pool, struct eval *val, int et)
     return 0;
 }
 
+#ifdef ANDROID_APP
+static int on_android_control(struct poolhd *pool, struct eval *val, int et)
+{
+    while (1) {
+        int client_fd = -1;
+        int udp_fd = -1;
+        int result = byedpi_android_receive_client(
+            val->fd, &client_fd, &udp_fd);
+        if (result > 0) {
+            return 0;
+        }
+        if (result < 0) {
+            pool->brk = true;
+            return 0;
+        }
+        struct eval *client = add_event(
+            pool, &on_request, client_fd, POLLIN);
+        if (!client) {
+            close(client_fd);
+            if (udp_fd >= 0) {
+                close(udp_fd);
+            }
+            continue;
+        }
+        client->addr.in.sin_family = AF_INET;
+        client->addr.in.sin_addr.s_addr = htonl(INADDR_LOOPBACK);
+        client->injected_udp_fd = udp_fd;
+    }
+}
+#endif
+
 
 int on_tunnel(struct poolhd *pool, struct eval *val, int etype)
 {
@@ -1015,13 +1088,22 @@ int on_ignore(struct poolhd *pool, struct eval *val, int etype)
 int start_event_loop(int srvfd)
 {
     server_fd = srvfd;
+    #ifdef ANDROID_APP
+    byedpi_android_listener_ready(srvfd);
+    #endif
     
     struct poolhd *pool = init_pool(params.max_open * 2 + 1);
     if (!pool) {
         close(srvfd);
         return -1;
     }
-    if (!add_event(pool, &on_accept, srvfd, POLLIN)) {
+#ifdef ANDROID_APP
+    evcb_t listener_cb = byedpi_android_control_mode()
+        ? &on_android_control : &on_accept;
+#else
+    evcb_t listener_cb = &on_accept;
+#endif
+    if (!add_event(pool, listener_cb, srvfd, POLLIN)) {
         destroy_pool(pool);
         close(srvfd);
         return -1;
@@ -1036,6 +1118,12 @@ int start_event_loop(int srvfd)
 
 int listen_socket(const union sockaddr_u *srv)
 {
+#ifdef ANDROID_APP
+    int control_fd = byedpi_android_take_control_fd();
+    if (control_fd >= 0) {
+        return control_fd;
+    }
+#endif
     int srvfd = nb_socket(srv->sa.sa_family, SOCK_STREAM);
     if (srvfd < 0) {
         uniperror("socket");  
diff --git a/proxy.h b/proxy.h
index 73fbfc4..ba62726 100644
--- a/proxy.h
+++ b/proxy.h
@@ -110,4 +110,15 @@ int start_event_loop(int srvfd);
 
 int run(const union sockaddr_u *srv);
 
+#ifdef ANDROID_APP
+void byedpi_android_listener_ready(int server_fd);
+int byedpi_android_take_control_fd(void);
+int byedpi_android_control_mode(void);
+int byedpi_android_receive_client(int control_fd, int *stream_fd, int *udp_fd);
+#endif
+
+int byedpi_shutdown_server(void);
+
+int byedpi_server_port(void);
+
 #endif
